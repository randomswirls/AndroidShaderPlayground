precision highp float;

uniform vec2 uScreen;
uniform vec2 uMousePos;
uniform float uTime;

// Just for throwing whatever in here to sanity check.
void main( )
{



	//vec2 ouv = gl_FragCoord.xy / uScreen.xy;

	gl_FragColor = vec4(1.0,1.0,1.0,1.0);
}